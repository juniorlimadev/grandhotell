<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">
    <changeSet id="1" author="system">
        <sql dbms="postgresql">
            INSERT INTO cargo (id_cargo, titulo) SELECT nextval('seq_cargo'), 'USER' WHERE NOT EXISTS (SELECT 1 FROM cargo WHERE titulo = 'USER');
            INSERT INTO cargo (id_cargo, titulo) SELECT nextval('seq_cargo'), 'ADMIN' WHERE NOT EXISTS (SELECT 1 FROM cargo WHERE titulo = 'ADMIN');
        </sql>
    </changeSet>
</databaseChangeLog>
